{
  "scripts": {
    "docs:dev": "npm run copy-spec && npm run diagrams && vitepress dev",
    "docs:build": "npm run copy-spec && vitepress build .",
    "docs:preview": "npm run copy-spec && vitepress preview",
    "copy-spec": "cp ../../poiesis/api/tes/spec/*.openapi.yaml public/openapi.yaml",
    "diagrams": "node scripts/render-diagrams.mjs"
  },
  "dependencies": {
    "@catppuccin/vitepress": "^0.1.0",
    "@ibm/plex": "^6.4.1",
    "@vercel/analytics": "^1.5.0",
    "swagger-ui": "^5.20.8",
    "vitepress-openapi": "^0.0.3-alpha.72"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.4.0"
  }
}
